console.log('hello');

const paintWalls = function (sides, color) {
    console.log("The " + sides + " wall has been painted " + color);
};

paintWalls("yellow");
paintWalls("blue");
paintWalls();
paintWalls("north", "blue");

const numberHight = function (number) {
    const value = 93
    if (number > 100) {
        return; ("True")
    } else if (number < 100) {
        return; ("False")
    };
};

const result = numberHight();
console.log(result);

const enteringPeople = function ('maximum', 'current', 'age') {
    if (maximum = > )
};

const maximum = 60
const current = 50
const age = 18

const result = enteringPeople();